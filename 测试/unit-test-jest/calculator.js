const calculator = {
    sum: (a, b) => {
        return a + b
    },
    diff: (a, b) => {
        return a - b
    },
    isNull: () => null,
    checkValue: (value) => value
}

module.exports = calculator